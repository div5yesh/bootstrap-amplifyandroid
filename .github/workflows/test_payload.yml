name: Test Payload

on:
  workflow_dispatch:
  pull_request:
    types: [closed]

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - name: Run echo command
        shell: bash
        run: echo '${{ github.event }}'
